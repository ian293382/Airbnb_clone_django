const apiService = {
    get: async function (url : string) : Promise<any> {
        console.log('get', url);


        // return new Promise((resolve, reject) => {

        // })
    }
}

export default apiService;